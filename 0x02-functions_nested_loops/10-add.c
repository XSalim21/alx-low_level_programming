#include "main.h"
/**
 * Function that adds two integers and returns results
 * @one: first number imput
 * @two: second number imput
 * Return: one + two
 *
 */
int add(int one,int two)
{
return (one + two);
}
